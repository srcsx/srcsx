<div dir="rtl">

<div dir='ltr'>

# 🤝 Contributing to SRCSX

</div>

سلام!  
از این‌که می‌خوای در توسعه‌ی **SRCSX** مشارکت کنی، واقعاً خوشحالم.  
برای اینکه همکاری‌ها سریع، استاندارد و بدون اصطکاک پیش بره، لطفاً این دستورالعمل‌ها را مطالعه و رعایت کن.

---

# 📌 قوانین کلی مشارکت

1. رفتار محترمانه و حرفه‌ای در Issues و PRها الزامی است.
2. قبل از ساخت PR:
   - Issue مرتبط را پیدا کنید یا بسازید.
   - راه‌حل را در Issue مطرح کنید (در صورت نیاز).
   - پروژه را fork کنید.
3. از ایجاد PR مستقیم روی `main` خودداری کنید.
4. هر تغییر باید در **branch مجزا** انجام شود.
5. کامیت‌ها باید **معنادار، مختصر و استاندارد** باشند:
   - feat: افزودن سیستم مدیریت فلوچارت
   - fix: رفع مشکل ناهم‌ترازی گراف
   - chore: بهبود رنگ‌بندی دکمه‌ها
   - refactor: بازنویسی سرویس دوره‌ها
6. ساختار دایرکتوری‌ها را بدون دلیل تغییر ندهید.
7. اگر روی بخش Data کار می‌کنید:
   - فرمت JSON / ساختار دایرکتوری‌ها باید ثابت بماند.
   - تغییرات breaking باید در Issue توضیح داده شوند.
8. اگر روی بخش Code کار می‌کنید:
   - از ESLint و Prettier مطابق تنظیمات پروژه استفاده کنید.
   - TypeScript types را نشکنید.

<br >

## 🛠️ مراحل Contribution

### ۱. Fork و Clone

```bash
git clone https://github.com/srcsx/srcsx.git && cd srcsx
npm install
cp .env.example .env
cp -r mock-data .data
docker compose up -d
npx prisma generate && npx prisma db push && npx prisma db seed
npm run dev
```

### ۲. ساخت Branch جدید

```bash
git checkout -b feature/my-feature
```

### ۳. اعمال تغییرات و Commit

```bash
git add .
git commit -m "feat: توضیح مختصر درباره‌ی تغییر"
```

### ۴. Push کردن Branch

```bash
git push origin feature/my-feature
```

###۵. باز کردن Pull Request

_عنوان PR باید واضح باشد._

توضیحات شامل:

`نوع تغییر` (feature / fix / chore / data-update)

`دلیل تغییر`

`Issue` مرتبط

## 🧪 تست، کیفیت و Code Style

قبل از ارسال PR مطمئن شوید:

- برای بخش Code
  پروژه بدون خطا build می‌شود.

- ESLint و Prettier بدون خطا اجرا می‌شوند.

- تغییرات باعث شکست وابستگی‌ها و APIهای مرتبط نشوند.

## 🌱 پیشنهاد ویژگی‌ها و ایده‌ها

اگر ایده یا بهبود جدیدی دارید:

- یک Issue با برچسب enhancement باز کنید.

جزئیات و مثال‌ها را اضافه کنید- .

- در صورت امکان، طرح اولیه یا mock پیشنهاد دهید.

---

# 📚 ساختار کلی پروژه

پروژه SRCSX شامل دو بخش اصلی است:

### ۱. بخش Code

کدهای پروژه در این بخش شامل سرویس‌ها، APIها، UI و ابزارهای مشترک است.  
معماری پروژه به صورت **Layered & Feature-based** است و هر بخش نقش مشخصی دارد:

- **Services (`services/`)**:  
  لایه‌ی منطق (business logic). هر سرویس وظیفه مشخصی دارد و مستقل از UI و API است، مثلا `getDocs`.

- **API (`app/api/`)**:  
  مسیرهای HTTP که سرویس‌ها را expose می‌کنند. مثلا `GET /api/docs` سرویس `getDocs` را کال می‌کند.

- **Pages & Features (`need-setup/` و `no-setup/`)**:  
  صفحات بر اساس نیاز به setup کاربر یا عمومی بودن دسته‌بندی شده‌اند. `need-setup` فقط زمانی قابل دسترسی است که اطلاعات کاربر موجود باشد.

- **Components (`components/`)**:
  - `layout/`: ساختار و لایه‌بندی صفحات.
  - `pages/`: کامپوننت‌های اختصاصی هر صفحه.
  - `utils/`: کامپوننت‌ها و ابزارهای عمومی مانند buttons و inputs.

> تغییرات در این بخش باید استانداردهای کدنویسی و معماری پروژه را رعایت کنند تا قابلیت توسعه و نگهداری پروژه حفظ شود.

### ۲. بخش Data

- شامل فایل‌ها، ساختارها و منابع مربوط به داده‌های آموزشی، دوره‌ها، روابط، دسته‌بندی‌ها و ...

- پروژه شامل داده‌ها و منابع آموزشی است که در دایرکتوری mock-data برای توسعه و تست در دسترس است.

#### فایل‌های اصلی (Global Data Files)

`universities.json`

**اطلاعات دانشگاه‌ها**

نمونه:

<div dir=ltr>

```json
{
  "id": 1,
  "name": "دانشگاه آزاد واحد علوم و تحقیقات",
  "slug": "srbiau"
}
```

</div>

`majors.json`

**اطلاعات رشته‌ها**

نمونه:

<div dir=ltr>

```json
{
  "id": 101, // مهم
  "universityId": 1,
  "type": "bachelor", // bachelor | master | phd
  "slug": "computer-science",
  "name": "علوم کامپیوتر"
}
```

</div>

`docs.json`

**اطلاعات راهنماها**

نمونه:

<div dir=ltr>

```json
{
  "name": "انتخاب واحد ترم عادی",
  "sections": [
    {
      "title": "زمان‌بندی انتخاب واحد",
      "message": "زمانبندی انتخاب واحد ترم های عادی هر ترم در کانال های دانشگاه اطلاع رسانی می‌شود."
    }
  ]
}
```

</div>

`reference_courses.json`

**اطلاعات درس‌های مرجع**

نمونه:

<div dir=ltr>

```json
{
  "id": 100100,
  "name": "فارسی",
  "unit": 3,
  "theoUnit": 3,
  "pracUnit": 0
}
```

</div>

`courses_resources.json‍`

**منابع مرتبط با درس های مرجع**

نمونه:

<div dir=ltr>

```json
{
  "courseReferenceId": 101100,
  "englishName": "Calculus: A Complete Course , 7th Edition – Robert Adams , Christopher Essex",
  "persianName": "حساب دیفرانسیل و انتگرال آدامز ویرایش هفتم (سال 2009)"
}
```

</div>

#### **دایرکتوری رشته‌ها (Majors)**

- دایرکتوری majors/ شامل یک پوشه برای هر رشته است.

- ساختار اسم پوشه‌ها:

  xxx-"uni-slug"-"degree"-"major"

  xxx: همان id رشته در majors.json

  uni-slug: slug دانشگاه

  degree: مقطع (bachelor / master / phd)

  major: slug رشته

  مثال:

  101-srbiau-bachelor-computer-science

**محتویات داخل هر major**

<br >

۲️⃣ `syllabi.json`

اطلاعات مربوط به سرفصل‌های رشته

نمونه:

<div dir=ltr>

```json
{
  "id": 101000,
  "majorId": 101,
  "name": "سرفصل 1400 و به بعد",
  "minEntryYear": 1400,
  "maxEntryYear": 1500,
  "hasFlowChart": true
}
```

</div>

`id`: شناسه سرفصل (شروع با کد رشته، مثلا 101 برای علوم کامپیوتر)

`majorId`: رشته مربوطه

`hasFlowChart`: مشخص می‌کند آیا فلوچارت دارد یا نه

<br >

۲️⃣ `files.json`

فایل‌های مربوط به رشته

نمونه:

<div dir=ltr>

```json
{
  "syllabusId": 101000,
  "title": "سرفصل مصوب کارشناسی ورودی 1400 و به بعد",
  "url": "https://s34.picofile.com/file/8487957126/cs_1400.pdf.html",
  "type": "syllabus"
}
```

</div>

`syllabusId`: لینک به سرفصل مربوطه

`type`: نوع فایل (یا chart syllabus)

<br >

۳️⃣ `courses.json`

اطلاعات دروس رشته

نمونه:

<div dir=ltr>

```json
{
  "id": 101100,
  "courseReferenceId": 101100,
  "name": "ریاضی عمومی (1)",
  "unit": 3,
  "theoUnit": 3,
  "pracUnit": 0,
  "amozeshyarCode": "6117165140"
}
```

</div>

`id`: شناسه درس (شروع با کد رشته، مثلا 101)

`courseReferenceId`: رفرنس اصلی درس در reference_courses.json

`unit` / `theoUnit` / `pracUnit`: واحد کلی - نظری - عملی

`amozeshyarCode`: کد آموزشیار

<br >

۴️⃣ `course_groups.json`

گروه‌بندی دروس هر سرفصل

نمونه:

<div dir=ltr>

```json
{
  "syllabusId": 101000,
  "type": "required", // required | optional | elective
  "name": "دروس پایه",
  "minRequiredUnits": 18,
  "maxRequiredUnits": 18,
  "minRequiredCourses": 7,
  "maxRequiredCourses": 7,
  "description": "هر 7 عنوان درسی به ارزش 18 واحد باید اخذ شوند.",
  "courses": [101100, 101101, 101102, 101103, 101104, 101105, 101106]
}
```

</div>

مشخص می‌کند چه دروسی در چه گروهی هستند و حداقل/حداکثر واحد و تعداد درس‌ها

<br >

۵️⃣ `course_requisites.json`

پیش‌نیازهای درسی

<div dir=ltr>

```json
{
  "syllabusId": 101000,
  "courseId": 101101,
  "courseRequisiteId": 101100,
  "corequisite": false
}
```

</div>

`courseId`: درس هدف

`courseRequisiteId`: درس پیش‌نیاز

corequisite: اگر همنیاز باشند true

<br >

۶️⃣ `course_unit_requisites`.json

پیش‌نیاز بر اساس واحد پاس شده

<div dir=ltr>

```json
{
  "syllabusId": 101000,
  "courseId": 101105,
  "unit": 60
}
```

</div>

نشان می‌دهد برای اخذ درس، حداقل چند واحد باید گذرانده شده باشد

<br >

۷️⃣ `terms_courses_syllabi.json`

درس‌های پیش‌فرض هر ترم در یک سرفصل

<div dir=ltr>

```json
{
  "syllabusId": 101000,
  "term": 1,
  "courses": [101100, 101102, 101104, 101106, 101110, 100104]
}
```

</div>

مشخص می‌کند چه درس‌هایی در ترم اول یا ترم‌های بعدی به صورت پیش‌فرض ارائه می‌شوند

<br >

_برای همکاری در بخش data با ایمیل من `hqmohammadreza@gmail.com` در ارتباط باشید._

---

## 🙏 قدردانی

از مشارکت شما در SRCSX متشکرم!
هر Contribution، هرچقدر کوچک یا بزرگ، به بهتر شدن پروژه کمک می‌کند. ❤️
